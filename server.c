#include <stdio.h>
#include <stdbool.h>

void readInput(){
        printf("thread readInput");
        // timeout 2 seconds
    }
    
    void receiveData(){
        printf("thread receiveData");
        // timeout 2 seconds
    }

int main (){
    printf("Enter IP address and port number of the client");
    printf("IP address is () on port ()");
    printf("Connection has been Established");
    
    bool exit = false;
    
    

    do{
        // Alternate threads between
        //  checking to receive input




    }while(exit != true);
}